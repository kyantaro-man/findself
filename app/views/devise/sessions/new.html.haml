.sign-up__wrapper
  .sign-up__box
    = image_tag "/assets/title-logo.png", alt: "新規登録ロゴ画像", height: "44px", class: "sign-up__box-logo"
    %h2.sign-up__box-title ログインして続ける
    = form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "sign-up__form"}) do |f|
      .sign-up__form-item
        = f.label :email
        %br/
        = f.email_field :email, autocomplete: "email", class: "sign-up__form-input", placeholder: "(例) yamada@gmail.com"
      .sign-up__form-item
        = f.label :password
        %br/
        = f.password_field :password, autocomplete: "current-password", class: "sign-up__form-input", placeholder: "パスワード"
      - if devise_mapping.rememberable?
        .field
          = f.check_box :remember_me
          = f.label :remember_me
      .sign-up__form-submit
        = f.submit "→", class: "sign-up__form-btn"